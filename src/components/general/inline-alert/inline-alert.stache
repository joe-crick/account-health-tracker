<div class="jumbotron">
    <h1>{{alertTitle}}</h1>
    <p>{{message}}</p>
    {{#if action}}
    <p>
        <button class="btn btn-primary btn-lg" ($click)="{action}">{{buttonLabel}}</button>
    </p>
    {{/if}}
</div>